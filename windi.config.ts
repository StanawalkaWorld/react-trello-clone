import { defineConfig } from "windicss/helpers";

export default defineConfig({
    darkMode: "media",
    theme: {
        extend: {
            fontFamily: {
                roboto: ["Roboto", "sans-serif"],
            },
        },
    },
});
